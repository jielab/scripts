name: saige
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-saige=1.3.1
  # The bioconda SAIGE binary uses classic TBB symbols; newer RcppParallel
  # switched to oneTBB and causes an undefined-symbol error on library(SAIGE).
  - r-rcppparallel=5.1.9
