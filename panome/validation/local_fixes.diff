diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/README.md /tmp/panome_v4_review_20260922_132704/README.md
--- /mnt/d/Downloads/Panome_v4_reference/panome/README.md	2026-09-22 13:24:09.698086800 +0800
+++ /tmp/panome_v4_review_20260922_132704/README.md	2026-09-22 13:31:11.916946728 +0800
@@ -4,6 +4,8 @@
 
 这是研究实现。真实 UKB 新版结果须在你的本地数据上重新运行；本包不包含这些结果。旧版结果的核查见 `AUDIT.md`。本版聚焦有首诊日期和随访的疾病，按指定时间窗预测 net risk；不提供旧版 height、Transformer、Leiden 或 PGS 接口。
 
+本机替换前的修复、真实输入抽样核查及运行记录见 [LOCAL_REVIEW.md](LOCAL_REVIEW.md)。
+
 ## 运行
 
 将整个 `panome/` 目录作为新版使用，避免混入旧 `f/`。默认输出到新的 `v4_reference` 子目录，不覆盖旧结果。
@@ -33,7 +35,7 @@
 - 临床协变量 `age,sex,tdi,PC1,PC2,center`；蛋白残差化 `age,sex,prot.plate`。
 - 死亡作为删失，输出是死亡删失下的 net risk，**不是竞争死亡下的实际累计发病概率**。
 
-RDS 优先用已有 `Rscript`，没有 R 时可另装 `pyreadr`。CSV、TSV、gzip 文本也可直接输入。不会 source LE8 或修改原始数据。`--tree lightgbm` 是默认树基准；未安装会报错，不会默默替换。可明确选 `--tree hist` 使用 sklearn 的 histogram gradient boosting。
+RDS 优先用已有 `Rscript` 提取字段，再由 `pyreadr` 读取二进制中间文件；`pyreadr` 已列入依赖，没有 R 时直接使用它读取。这样可保留批次等分类元数据中的极小数值。CSV、TSV、gzip 文本也可直接输入。不会 source LE8 或修改原始数据。`--tree lightgbm` 是默认树基准；未安装会报错，不会默默替换。可明确选 `--tree hist` 使用 sklearn 的 histogram gradient boosting。
 
 亲缘连通分量列已准备好时，请加 `--group-col family_component`。这样外层划分、内层 OOF、匹配排除和 bootstrap 都以亲缘组处理。没有亲缘信息时是个人随机划分，不能声称已排除亲缘泄漏。
 
diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/f/export_rds.R /tmp/panome_v4_review_20260922_132704/f/export_rds.R
--- /mnt/d/Downloads/Panome_v4_reference/panome/f/export_rds.R	2026-09-22 13:24:09.725090500 +0800
+++ /tmp/panome_v4_review_20260922_132704/f/export_rds.R	2026-09-22 13:28:43.948970302 +0800
@@ -14,8 +14,5 @@
     x[[nm]] <- format(x[[nm]], "%Y-%m-%d")
   if (inherits(x[[nm]], "integer64")) x[[nm]] <- as.character(x[[nm]])
 }
-if (requireNamespace("data.table", quietly = TRUE)) {
-  data.table::fwrite(x, args[2], na = "")
-} else {
-  write.csv(x, args[2], row.names = FALSE, na = "")
-}
+# Binary transfer preserves subnormal numeric metadata used as category codes.
+saveRDS(x, args[2], compress = FALSE)
diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/f/io_data.py /tmp/panome_v4_review_20260922_132704/f/io_data.py
--- /mnt/d/Downloads/Panome_v4_reference/panome/f/io_data.py	2026-09-22 13:24:09.731090700 +0800
+++ /tmp/panome_v4_review_20260922_132704/f/io_data.py	2026-09-22 13:28:43.948970302 +0800
@@ -34,12 +34,13 @@
         if shutil.which(r_bin):
             # R selects columns before serialization, avoiding a second full RDS in Python.
             with tempfile.TemporaryDirectory(prefix="panome_rds_") as tmp:
-                out = Path(tmp) / "selected.csv"
+                out = Path(tmp) / "selected.rds"
                 cols = Path(tmp) / "columns.txt"
                 cols.write_text("\n".join(columns or []))
                 subprocess.run([r_bin, str(Path(__file__).with_name("export_rds.R")),
                                 str(path), str(out), str(cols)], check=True)
-                frame = pd.read_csv(out, dtype={id_col: str}, low_memory=False)
+                import pyreadr
+                frame = pyreadr.read_r(str(out))[None].reset_index(drop=True)
         else:
             import pyreadr
             objects = pyreadr.read_r(str(path))
@@ -228,3 +229,4 @@
     dump(out / "input_audit.json", dict(n=len(p), features=len(features),
          source="SYNTHETIC" if a.demo else upstream, met_input=a.met_input,
          missing_fraction=float(np.mean(~np.isfinite(x)))))
+    return p
diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/f/panome.py /tmp/panome_v4_review_20260922_132704/f/panome.py
--- /mnt/d/Downloads/Panome_v4_reference/panome/f/panome.py	2026-09-22 13:24:09.738088100 +0800
+++ /tmp/panome_v4_review_20260922_132704/f/panome.py	2026-09-22 13:28:28.944972696 +0800
@@ -9,7 +9,15 @@
 
 
 def parser():
-    p = argparse.ArgumentParser(description=__doc__)
+    p = argparse.ArgumentParser(description=__doc__,
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+        epilog="""Usage examples:
+  ./panome.sh --Y cvd_cad --biom prot --dry-run
+  ./panome.sh --Y cvd_cad --biom prot --preflight
+  ./panome.sh --Y cvd_cad --biom prot --cores 16
+  ./panome.sh evaluate --run-dir /mnt/d/analysis/panome/cvd_cad/prot/v4_reference
+  ./panome.sh --demo --tree hist --max-samples 1600 --run-name synthetic_check --analysis-root /tmp/panome_check
+""")
     p.add_argument("command", nargs="?", choices=["run", "evaluate", "project"], default="run")
     p.add_argument("--Y", dest="trait", default="cvd_cad")
     p.add_argument("--biom", choices=["prot", "met"], default="prot")
@@ -143,11 +151,15 @@
         paths = [] if a.demo else [a.phe_file, a.omics_file]
         if a.module_file:
             paths.append(a.module_file)
+        if a.split_file:
+            paths.append(a.split_file)
+        if a.biom == "met" and a.met_input == "raw" and not a.demo:
+            paths.append(a.met_map)
         missing = [v for v in paths if not Path(v).is_file()]
         if missing:
             raise FileNotFoundError("Missing inputs: "+", ".join(missing))
-        if any(str(v).endswith(".rds") for v in paths) and not shutil.which(a.r_bin) and not importlib.util.find_spec("pyreadr"):
-            raise RuntimeError("RDS needs Rscript or pyreadr")
+        if any(str(v).lower().endswith(".rds") for v in paths) and not importlib.util.find_spec("pyreadr"):
+            raise RuntimeError("RDS needs pyreadr for lossless binary reading; install requirements.txt")
         if a.tree == "lightgbm" and not importlib.util.find_spec("lightgbm"):
             raise RuntimeError("LightGBM missing; install requirements.txt or explicitly choose --tree hist")
         log("DONE", "preflight", "paths and required reader/tree dependencies available; data values not yet read")
diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/f/pipeline.py /tmp/panome_v4_review_20260922_132704/f/pipeline.py
--- /mnt/d/Downloads/Panome_v4_reference/panome/f/pipeline.py	2026-09-22 13:24:09.743090300 +0800
+++ /tmp/panome_v4_review_20260922_132704/f/pipeline.py	2026-09-22 13:28:28.940972695 +0800
@@ -73,7 +73,9 @@
         inputs.append(a.met_map)
     return dict(version=VERSION, config=vars(a), dependencies=deps,
                 inputs=fingerprints(inputs, a.full_input_hash),
-                code=fingerprints(Path(__file__).parent.glob("*.py"), True))
+                code=fingerprints(list(Path(__file__).parent.glob("*.py")) +
+                                  [Path(__file__).with_name("export_rds.R"),
+                                   Path(__file__).parent.parent/"panome.sh"], True))
 
 
 def train(a, out):
@@ -81,8 +83,8 @@
     log("START", "prepare")
     prepared = out/"input"
     prepared.mkdir(exist_ok=True)
-    prepare(a, prepared)
-    p = pd.read_csv(prepared/"phenotype.csv", dtype={a.id_col: str})
+    # Keep original metadata types/values; CSV is an audit export, not model input.
+    p = prepare(a, prepared)
     raw = np.load(prepared/"raw.npy")
     features = (prepared/"features.txt").read_text().splitlines()
     p, audit = outcomes(p, a)
diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/f/report.py /tmp/panome_v4_review_20260922_132704/f/report.py
--- /mnt/d/Downloads/Panome_v4_reference/panome/f/report.py	2026-09-22 13:24:09.754087200 +0800
+++ /tmp/panome_v4_review_20260922_132704/f/report.py	2026-09-22 13:28:28.944972696 +0800
@@ -52,7 +52,7 @@
     for ext in ["png", "pdf"]:
         fig.savefig(out/f"Fig_prediction_coverage.{ext}", dpi=180)
     plt.close(fig)
-    pairs = pd.read_csv(out/"same_risk_pairs.csv")
+    pairs = pd.read_csv(out/"same_risk_pairs.csv", dtype={"eid_A": str, "eid_B": str})
     profiles = pd.read_csv(out/"molecular_profiles.csv", dtype={0: str})
     if len(pairs):
         idcol = profiles.columns[0]
diff -ru '--exclude=.venv' '--exclude=__pycache__' '--exclude=validation' '--exclude=LOCAL_REVIEW.md' /mnt/d/Downloads/Panome_v4_reference/panome/requirements.txt /tmp/panome_v4_review_20260922_132704/requirements.txt
--- /mnt/d/Downloads/Panome_v4_reference/panome/requirements.txt	2026-09-22 13:24:09.709086800 +0800
+++ /tmp/panome_v4_review_20260922_132704/requirements.txt	2026-09-22 13:28:28.944972696 +0800
@@ -6,4 +6,5 @@
 threadpoolctl>=3.2,<4
 matplotlib>=3.8,<4
 lightgbm>=4.3,<5
-# RDS: use the existing Rscript; pyreadr is optional when R is unavailable.
+pyreadr>=0.5,<0.6
+# RDS: Rscript selects and normalizes columns; pyreadr reads the binary transfer.
