# Local environment

Do not create a `.venv` directory or symlink in this project. The user explicitly
requested its removal and does not want it recreated. Use `PANOME_PYTHON` or the
existing external interpreter selected by `panome.sh`; do not bootstrap a new
environment automatically.
